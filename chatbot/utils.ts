export function processMessage(message: string, config: any): string {
  // Implement the logic to process the message
  return `Processed message: ${message}`;
}

export function formatResponse(response: string): string {
  // Implement the logic to format the response
  return `Formatted response: ${response}`;
}
