# Verificar o histórico de commits

Para ver o histórico de commits de um branch ou ficheiro usa:

```
git log [opções] [caminho]
```

Exemplo: `git log --oneline -n 5` mostra os 5 commits mais recentes num formato compacto.
