# Instalar o Git no Ubuntu

No Ubuntu, instala o Git com o gestor de pacotes `apt`:

```
sudo apt update
sudo apt install git
```

Verifica a instalação com:

```
git --version
```

Após instalar, configura o teu nome e email com `git config --global user.name` e `git config --global user.email`.
