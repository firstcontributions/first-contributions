# 양돈 AI — 현황 브리핑 · 남은 계획

기준일 **2026-08-13** · 마감 8월 말 → **약 18일** 남음
브랜치 `claude/yangdon-ai-competition-3x2ukm`

다른 대화로 옮겨 붙일 수 있게 자립적으로 적는다. 이 문서만 읽어도
프로젝트가 어디까지 왔고 무엇이 비었는지 알 수 있어야 한다.

---

## 1. 한 줄

> 상용은 **기록·탐지**까지. 이 프로젝트는 **진단·설계**다 —
> "왜 못 나오는지, 얼마 손해인지, 몇 원인지, 지을 방이 있는지."

CCTV 발정 탐지에서 출발했지만, 국내 실측을 재 보니 손실이 발정에만 있지
않아 **농장 전체 관리**로 범위를 넓혔다. 그 전환의 근거가 데이터에 있다.

---

## 2. 지금 서 있는 자리

**규모** 모듈 64개 · 대시보드 뷰 21개 · 테스트 66개(전부 통과)
자체완결 HTML(외부 연결 0) · `python competition/tests/smoke_test.py`

### 손에 쥔 데이터

| 자산 | 규모 | 상태 |
|---|---|---|
| 국내 농장 연간 성적 | **202농장 × 4년 = 466행** (2020~2023) | ✅ 분석 완료 |
| 국내 농장 월별 성적 | **179농장** · 분만율은 68농장 (2020·2021) | ✅ 농장별 분석 완료 |
| AI Hub 71471 발정행동 | 33,600파일 서브셋 | ✅ 실측 검증 완료 |
| AI Hub 622 폴리곤 라벨 | 8MB (원천 10GB 는 미수신) | ✅ 상한 측정 완료 |
| 케글 3종(행동·자세·탐지) | 45,611 bbox 등 | ✅ 학습·검증 완료 |
| 캐글 GPU 딥러닝 3판 | 자세 CNN · 행동 시퀀스 | ✅ 완료 — 아래 ⑦ |

원자료 스프레드시트는 **농장 식별자가 있어 미커밋**. 집계 JSON 만 커밋한다.

### 검증된 핵심 수치 — 발표에서 쓸 것

**① 격차는 국가가 아니라 농장 사이에 있다** (466행)

| | PSY | MSY | 이유후 육성률 |
|---|---|---|---|
| 국내 하위 25% | 21.9 | 14.9 | 74.7% |
| 국내 중앙 | 24.1 | 18.8 | 83.8% |
| **국내 상위 10%** | **27.9** | **25.7** | **93.9%** |
| 덴마크(문헌) | 31.3 | 29.2 | 93.3% |

상위 10% 는 육성률에서 덴마크를 이미 따라잡았다 → 고칠 수 있는 문제다.

육성률 열만 계산 방식이 다르다. **농장별 MSY/PSY 의 분포**(95농장)이지
분위수끼리 나눈 값이 아니다. 그렇게 냈다가 92.1% 로 틀렸었다 — PSY 448행
vs MSY 127행이라 모집단이 다르고, 그런 농장은 실재하지 않는다. 119농장 중
24개는 육성률 60~100% 밖(최대 207.8%)이라 보고 오류로 뺐다.

**② PSY 를 가르는 건 발정 관리가 만드는 값이다**

NPD ρ −0.737 · 분만율 ρ +0.563 · 회전율 ρ +0.727 · 재귀발정일 ρ −0.365.
임신사고의 **66.9% 가 재발**(1차 35.3 + 불규칙 19.9 + 2차 11.7).

**③ 여름 불임은 교배월로 되돌려야 보인다** (179농장 월별)

분만율 여름 교배 81.1% vs 겨울 84.1% = **−2.97%p**. 분만 시점 그대로 보면
11월이 최저라 안 보인다. 임신 114일을 빼야 7·8월이 드러난다.

**③′ 여름 손실은 농장마다 다르다 — 공통 처방이 아니라 선별 처방** ← 오늘 나온 것

원자료 **6,533행 중 유일한 건 1,464행**(77.6% 가 중복)이었다. 값이 다른
중복은 0건이라 지워도 안전하지만, 반복 횟수가 농장마다 1~14회로 달라
농장별로 쪼개면 성립하지 않는다. 지우고 다시 낸 게 위 −2.97%p 다(전 −2.70).

분만율을 실제로 쓸 수 있는 건 **68농장 · 893농장-월**(2020·2021). 그중
계절 손실(겨울 교배 − 여름 교배)을 낼 수 있는 **67농장**의 분포:

| | p10 | 중앙 | p90 | SD |
|---|---|---|---|---|
| 관측 | −4.4 | +2.7 | +13.0 | 7.75 |
| **표본 오차 제거 후** | **−0.5** | **+3.5** | **+7.8** | **4.94** |

관측 분산의 **41% 만 진짜 농장 차이**고 나머지는 표본 오차다(농장마다
계절당 3개월). 걷어내도 분포는 **사라지지 않는다** — 여름에 8%p 무너지는
농장과 0%p 인 농장이 같이 있다.

**원/년(손실 상한, 300두 환산)** 중앙 **733만원** · 상위10% **3,172만원**.
67농장 실제 규모 합 **9.7억원/년**. PSY 1두(3,751만원)·하락 방어(2,976만원)와
같은 축이다. 겨울 수준을 기준으로 잡은 값이라 장비값을 뺀 순이익이 아니다.

**무너지는 경로**는 사양이 아니라 착상이다. 여름에 평균이유 −0.25두,
7일내재귀율 −0.2%p 로 거의 안 움직이는데, 임신사고 구성이 **1차 재발 쪽으로
+8.0%p** 기운다(겨울 33.4% → 여름 41.4%). 발견 ②의 재발 66.9% 와 같은 항목이다.

**계절 취약도는 연간 성적·규모와 붙지 않는다**(PSY ρ −0.149 · 상시모돈
ρ −0.163 · n 67). 잘하는 농장도 여름은 피하지 못한다.

**③″ 임신 114일을 데이터가 스스로 찾았다 — 그리고 lag 회귀는 못 찾았다**

되돌림 개월을 0~6 으로 훑어 여름−겨울 대비가 가장 커지는 지점을 찾았다.
손으로 넣은 4개월을 빼고 데이터에 고르게 한 것이다:

```
0개월 −0.17   1 −1.45   2 −2.07   3 −3.48   4 −2.97   5 −1.15   6 +0.17
```

농장 단위 부트스트랩 400회에서 최댓값이 **3~4개월에 드는 비율 90%**
(3개월 57% · 4개월 33%). **임신 114일 = 3.75개월**을 데이터가 브래킷했다.
argmax 는 3개월이지만 **4개월을 그대로 쓴다** — 3으로 바꾸면 간판 수치가
−2.97 에서 −3.48 로 커지지만, 그건 생물학이 아니라 우리 수치를 키우는 선택이다.

반대로 **lag 회귀에서는 3~4 봉우리가 서지 않았다.** 486행·67농장,
농장 단위 GroupKFold(5) · 누수 fold 0:

| | MAE | B1 대비 |
|---|---|---|
| B0 전체 평균 | 8.576 | −39.7% |
| **B1 농장별 과거 평균** | **6.139** | 기준선 |
| B2 직전월 | 6.047 | +1.5% |
| B3 lag 1~6 선형 | **5.521** | **+10.1%** |
| B4 lag 1~6 부스팅 | 5.883 | +4.2% |

표준화 계수 봉우리도 순열 중요도 봉우리도 **lag1** 이고, lag3·lag4 는 구간이
0 을 물었다(β 0.90 [−0.12, 1.91] · 0.17 [−0.86, 1.19]). 사후에 보면 당연하다 —
분만율의 **자기상관**에 임신기간이 실릴 이유가 없다. 되돌림 스캔이 기전에
곧게 붙는 검증이고, lag 회귀는 그 자리에 쓸 도구가 아니었다.

lag 회귀 자체는 기준선을 이긴다(+10.1%). 다만 MAE 5.5%p 는 계절 손실
2.97%p 보다 크다 — **월 단위 분만율 예측은 아직 처방으로 쓸 정확도가 아니다.**

**④ 하락은 사양이 아니라 발정·교배 관리에서 온다** ← 오늘 나온 것

같은 농장 전년 대비(239쌍·122농장), 전년 성적을 맞춘 중간층:

| | 건수 | ΔPSY | ΔNPD | Δ이유두수 | Δ분만율 |
|---|---|---|---|---|---|
| 상승 | 29 | +1.80 | −3.2 | **+1.00** | +2.6 |
| 하락 | 35 | −2.20 | **+11.6** | **+0.00** | −3.8 |

떨어질 때 이유두수는 안 움직인다. 무너지는 폭이 올리는 폭의 3.6배.
농장-연의 **33% 가 1두 이상 떨어진다**(중앙 −2.40두) → 방어 기댓값
**연 2,976만원**(300두), PSY +1두(3,751만원)와 같은 급.

**⑤ 이 프로그램은 자기 낙관 폭을 스스로 신고한다** ← 오늘 나온 것

PSY 27.69(프로그램 가정) vs 25.34(중앙 농장) = **+2.36두 낙관**.
그중 NPD 하나가 −2.02두 — 설계 산식이 재발정·유산을 0 으로 깔고 있다.

**⑥ 정직하게 낸 부정 결과 둘** (이게 오히려 신뢰를 만든다)

- **71471 서브셋으로는 발정 판정 불가**. 카메라·개체 통제 시 AUC 0.465.
  원인은 모델이 아니라 라벨 어휘(발정 지시 행동이 주석에 없음)
- **폴리곤이 bbox 보다 낫지 않다**. 정답 폴리곤을 써도 MF1 −0.018 →
  10GB 다운로드와 14시간 CPU 학습을 안 해도 된다는 결론을 8MB 로 냈다

**⑦ 딥러닝 세 판 — 이겼다 · 졌다 · 주장을 고쳤다** (캐글 T4 실측)

| 실험 | 결과 |
|---|---|
| 자세 3클래스 (resnet18) | 0.636 → **0.732** / MF1 0.434 → 0.534 ✅ |
| 자세 → 발정 AUC 전파 | **+0.004** (0.709→0.713) · 시드 SD 0.019 안쪽 ❌ |
| 행동 시퀀스 1D-CNN | **0.427** vs 손피처 RF **0.485** ❌ |

**갈린 이유는 하나다 — 요약 피처가 원본의 정보를 버렸는가.**
자세는 60차원 요약이 머리 방향·질감을 버려서 CNN 이 이기고, 행동은
`speed`·`accel`·롤링 통계가 시간 정보를 이미 담아서 DL 이 진다.
표본 12,646행·96개체로는 CNN 이 그걸 다시 배우지 못한다.

**그리고 간판 주장 하나를 고쳤다.** "좌/우는 원리상 구분 불가 → 5클래스
상한 0.861" 이라고 써 왔는데:
- 좌/우 **한정** 정확도 **0.699**(동전 0.5) — 픽셀엔 방향 정보가 있다.
  "원리상" 이 아니라 **"bbox 로는"** 이었다
- CNN 5클래스가 0.557 로 상한에서 한참 아래다 — **상한은 병목도 아니었다**
- 진짜 병목은 좌/우가 아니라 **횡와↔복와↔기립 혼동**(정답 좌/우 중 27.1%
  만 좌/우로 예측, 나머지는 복와 39.4%·기립 30.3% 로 샌다)

---

## 3. 구멍 — 솔직하게

| # | 구멍 | 왜 문제인가 |
|---|---|---|
| 🔴 1 | **API 키 재발급 미완** (AI Hub · 케글) | 대화 기록에 노출됐다. **커밋 이력에는 0건**이라 이력 재작성은 불필요하고, 브라우저에서 재발급만 하면 된다 |
| ✅ 2 | ~~실측 근거가 화면에 없다~~ | **닫힘.** 20번째 뷰 `farm_diagnosis.html` — 466농장 분포·격차·하락 비대칭·계절 되돌림·자기 검증 5패널 |
| 🟠 3 | **발정 AUC 0.465 를 자동 검사할 수 없다** | 산출 JSON 이 없고, 다시 내려면 71471 원자료 33,600파일이 필요한데 커밋 대상이 아니다. 문서의 다른 수치는 `check_docs` 가 대조하는데 **이 하나만 사람이 지켜야 한다** |
| ✅ 4 | ~~월별 179농장을 절반만 썼다~~ | **닫힘.** 발견 ③′ — 67농장 계절 손실 분포·표본 오차 분해·원/년, 20번째 뷰 패널 4 에 반영. 남은 한계는 **분만율이 68농장·2020~2021 뿐**이라는 것(월별 파일에 2022 가 없고 2023 은 분만율이 없다) |
| 🟡 5 | `run_farm` ③단계가 실제 도면이 아님 | 단계별 두수를 **번식주기 비율로 유도**한 값이다(24.1/54.5/21.4%). 실제 농장 도면·개체 이력이 들어오면 그 농장 값으로 바뀐다 |
| 🟡 6 | 실농장 실증 없음 | 구조적 한계. 숨기지 말고 먼저 말하는 쪽으로 정리해 둠 |
| ⬜ 7 | 다단계(1/2/3-site)·SEW 미모델링 | 있으면 좋지만 없어도 논지는 선다 |

---

## 4. 남은 18일 계획

원칙: **새 기능보다 이미 가진 근거를 보이게 하는 데** 시간을 쓴다.
검증된 수치는 충분하고, 그게 화면에 없는 게 문제다.

### 1주차 (8/13~8/19) — 구멍 메우기

| 순 | 할 일 | 예상 | 왜 지금 |
|---|---|---|---|
| ✅ P0 | 시크릿 위생 | — | 키는 **커밋 이력에 0건**. 가중치 26MB 추적 해제, 무시 규칙을 확장자로. 남은 건 브라우저 재발급뿐 |
| ✅ P0 | README 첫 문단·제목 리프레임 | — | "발정 파이프라인" → "농장 진단·설계". 한계를 첫 화면에 |
| ✅ P1 | **실측 진단 대시보드 (20번째 뷰)** | — | `farm_diagnosis.html` · 패널 5개 · 외부 연결 0 |
| ✅ P1 | 월별 패널 분석 → 냉방 투자 값 | — | 구멍 #4 닫힘. 발견 ③′ · 원자료 중복 77.6% 도 여기서 잡았다 |

**1주차에 계획에 없던 것 셋을 같이 잡았다** — 셋 다 P1 작업 중에 근거를
확인하다 드러났다.
- 간판 수치 육성률 92.1% 가 **분위수끼리 나눈 값**이었다 → 농장별 재계산 93.9%
- `run_farm` ③단계를 "미검증 난수" 로 적어 놨는데 **난수가 아니었다**(결정론적)
- 진단 기본값이 실측 중앙값이라 **격차가 늘 0.00** 으로 찍히고 있었다

**20번째 뷰 — 들어간 것** (`build_farm_diagnosis.py` · 계산 없음, 렌더링만)
1. 466농장 PSY·NPD·이유두수·분만율 분포 + 내 농장 위치 — 순위 아닌 **거리**
2. 지표별 격차 → PSY 회수량 → 원/년 (`farm_gap`)
3. 상승/하락 비대칭 + 방어 기댓값 (`farm_panel`)
4. 계절 곡선 — 되돌리기 전/후를 **한 축에 동시 표시**(토글이면 안 눌러 본다)
5. 프로그램 자기 가정 vs 실측 (자기 검증)

패널마다 출처 라벨(실측/계산)을 박았고, 예시 농장에는 "실제 농장이 아니다"
를 명시했다. 외부 URL 0 · 농장 식별자 0 — 테스트가 확인한다.

### 2주차 (8/20~8/26) — 다듬기

| 순 | 할 일 | 예상 |
|---|---|---|
| P2 | `synth_farm` → `run_farm` ③ 배선 | 2시간 |
| P2 | 발표 리허설 · 슬라이드 시간 배분 | 반나절 |
| P2 | 예상 질문 대응 보강(부록 C) | 2시간 |
| P3 | 다단계·SEW (시간 남으면) | 하루 |
| ⬜ | 횡와↔복와 혼동 직접 겨냥 — 3클래스로 뭉치면 사라져 응용 영향 없음 | 낮음 |

### 3주차 (8/27~8/31) — 잠그기

- 전체 테스트 · `check_docs` 재확인
- 문서 수치 동결, 이후 코드 수정 금지
- 제출물 점검: 키 없음 · 저작권 자료 없음 · 식별자 없음

---

## 5. 절대 잊으면 안 되는 것

- 🔴 **API 키를 커밋하지 않는다.** `.gitignore` 에 `.aihub_key`, `.env`, `*.key`
- 🔴 **외부 유튜브 영상은 앱·제출물에 넣지 않는다** (저작권). 참고만
- 🔴 **농장 식별자가 든 원자료 xlsx 는 커밋 금지.** 집계 JSON 만
- 🔴 Edinburgh 데이터는 **CC BY-NC** — 커밋하지 않고 스크립트로 받는다
- ⚪ 모델 가중치(`models/*.pt`)도 gitignore

---

## 6. 지켜 온 원칙 — 흔들지 말 것

이 프로젝트의 신뢰는 수치가 높아서가 아니라 **구분을 지켜서** 생겼다.

1. **실측 / 계산 / 가정 / 합성을 항상 구분해 표시한다.** 출력 끝마다 찍는다
2. **낙관 편향을 만드는 검증은 폐기한다.** 누수된 0.642 를 버리고 0.467 을 썼다
3. **기준선을 먼저 찍는다.** 다수 클래스만 찍는 정확도보다 낮으면 개선이 아니다
4. **부정 결과를 보고한다.** 폴리곤 무용·발정 판정 불가가 오히려 근거다
5. **확인하지 않은 것을 단정하지 않는다.** 경쟁사 비교표를 그래서 뺐다
6. **스스로 잡은 오류를 슬라이드에 싣는다** (슬라이드 12)

---

## 7. 바로 돌려 볼 명령

```bash
cd /path/to/first-contributions
python competition/tests/smoke_test.py              # 66/66

python competition/src/run_farm.py --sows 300       # 설계~손익 6단계
python competition/src/farm_gap.py --program --sows 300   # 가정 vs 실측
python competition/src/farm_panel.py --sows 300     # 연도별 변화·하락 비대칭
python competition/src/farm_monthly_panel.py --audit    # 월별 원자료 감사
python competition/src/farm_monthly_panel.py --season   # 농장별 계절 손실 → 원/년
python competition/src/farm_monthly_panel.py --model    # lag 기준선 · 114일 검증
python competition/src/korean_farm_stats.py         # 466행 집계
python competition/src/farm_monthly.py              # 월별 계절성

bash competition/build_all.sh                       # 대시보드 21뷰 + 허브
python competition/src/ml_core.py                   # DL 과제별 가용 점검
python competition/tools/check_docs.py              # 문서 수치 대조
```
